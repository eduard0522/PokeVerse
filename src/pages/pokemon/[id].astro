---
import type { GetStaticPaths } from "astro";
import { getPokemonData } from "../../utilities/getData";
import Layout from "../../layouts/Layout.astro";
import PokemonDescriptionCard from "@components/pokemons/PokemonDescriptionCard/PokemonDescriptionCard.astro";



export const getStaticPaths = ( async () => {
  const paths = await getPokemonData("id")
  return paths;
}) satisfies GetStaticPaths;

const { name , url , descripcion , types , weight , height } = Astro.props
const currentPage = Astro.url.pathname;
---

<Layout title= { name }>
  <section aria-label=`Datos de Pokémon ${name}` class="flex flex-col justify-center items-center gap-4  pb-12">
      <div class="flex flex-col">
         <PokemonDescriptionCard name={name} url={url} description = {descripcion} types = {types} weight={weight} height={height} />
      </div>
      <div class="flex items-center justify-center w-full">
        <a href="/" class="text-2xl bg-clip-text text-transparent bg-gradient-to-br from-sky-200 to-sky-400 py-1 px-4 rounded-2xl underline "> Ver lista </a>
    </div>
  </section>
 
</Layou>